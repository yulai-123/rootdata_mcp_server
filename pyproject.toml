[project]
name = "rootdata-mcp-server"
version = "0.1.0"
description = "A small MCP server that exposes Rootdata Web3 project data to AI assistants"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]
